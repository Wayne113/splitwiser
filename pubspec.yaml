name: splitwiser
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  path_provider: ^2.1.2
  camera: ^0.10.5+9
  image_picker: ^1.0.7
  intl: ^0.20.2
  shared_preferences: ^2.0.15 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
